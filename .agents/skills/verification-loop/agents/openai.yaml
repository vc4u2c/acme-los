interface:
  display_name: 'Verification Loop'
  short_description: 'ACME LOS prettier, lint, test, build, E2E, and deploy checks'
  brand_color: '#10B981'
  default_prompt: 'Run the ACME LOS verification loop for this change and report readiness.'
policy:
  allow_implicit_invocation: true
