---
name: api-design
description: REST API design guidance for production endpoints, resource naming, status codes, errors, pagination, and rate limits.
---

# API Design

This Claude-compatible skill mirrors `.agents/skills/api-design/SKILL.md`.

When this skill triggers:

1. Read `.agents/skills/api-design/SKILL.md`.
2. Follow that workflow as the source of truth.
3. Resolve any relative `scripts/`, `references/`, or `assets/` paths from `.agents/skills/api-design/`.
4. Adapt Codex-specific tool names to the equivalent Claude Code capabilities.
