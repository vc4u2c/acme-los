---
name: backend-patterns
description: Backend architecture patterns for Node, Express, Next route handlers, the .NET BFF, APIs, logging, and data access.
---

# Backend Patterns

This Claude-compatible skill mirrors `.agents/skills/backend-patterns/SKILL.md`.

When this skill triggers:

1. Read `.agents/skills/backend-patterns/SKILL.md`.
2. Follow that workflow as the source of truth.
3. Resolve any relative `scripts/`, `references/`, or `assets/` paths from `.agents/skills/backend-patterns/`.
4. Adapt Codex-specific tool names to the equivalent Claude Code capabilities.
