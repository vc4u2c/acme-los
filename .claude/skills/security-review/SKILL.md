---
name: security-review
description: Security review workflow for auth, secrets, input validation, APIs, sensitive data, telemetry, and dependencies.
---

# Security Review

This Claude-compatible skill mirrors `.agents/skills/security-review/SKILL.md`.

When this skill triggers:

1. Read `.agents/skills/security-review/SKILL.md`.
2. Follow that workflow as the source of truth.
3. Resolve any relative `scripts/`, `references/`, or `assets/` paths from `.agents/skills/security-review/`.
4. Adapt Codex-specific tool names to the equivalent Claude Code capabilities.
