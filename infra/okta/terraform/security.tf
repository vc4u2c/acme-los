# Intentionally empty for now.
#
# The environment manifests still track customer-group, authenticator, and
# policy intent, but those admin-plane writes stay out of the Terraform apply
# set until the Okta provider's service-app OAuth support is reliable for them.
